# Results

This folder contains curated benchmark outputs that are small enough to version safely:

- `benchmark_20k/`
- `benchmark_20k_round2/`
- `dataset_manifest_20k.json`

The raw local working directories under `artifacts/` are intentionally ignored by git.

Key outcome:

- best overall model on the current reproduced setup:
  - `logreg_tfidf_valuechar_effective_tuned`
- best neural model on the current reproduced setup:
  - `securebert2_lora_balanced_softmax_effective_epoch3_temp`
